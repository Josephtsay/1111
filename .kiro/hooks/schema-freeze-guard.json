{
  "version": "v1",
  "hooks": [
    {
      "name": "Schema v0.1 freeze guard",
      "trigger": "PreToolUse",
      "description": "Asks for confirmation only when writing to files that define node/edge types, ID rules, or quality policy. Silent for docs and analysis scripts.",
      "matcher": "fs_write|str_replace|fs_append",
      "action": {
        "type": "command",
        "command": "python3 .kiro/hooks/scripts/schema_guard.py",
        "timeout": 20
      }
    }
  ]
}