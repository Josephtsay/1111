{
  "version": "v1",
  "hooks": [
    {
      "name": "Git staging safety",
      "trigger": "PreToolUse",
      "description": "Blocks unsafe git add/commit (gitignored paths, >100KB files, git add . / -A / -f). Silent for every other bash command.",
      "matcher": "execute_bash",
      "action": {
        "type": "command",
        "command": "python3 .kiro/hooks/scripts/git_guard.py",
        "timeout": 20
      }
    }
  ]
}